class Underweight {
  int underwID;
  String underwName;
  String desc;
  bool isSelected;
  int status;

  Underweight(
      {required this.underwID,
        required this.underwName,
        required this.desc,
        required this.isSelected,
        required this.status});
}
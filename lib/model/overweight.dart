class Overweight {
  int overwID;
  String overwName;
  String desc;
  bool isSelected;
  int status;

  Overweight(
      {required this.overwID,
        required this.overwName,
        required this.desc,
        required this.isSelected,
        required this.status});
}